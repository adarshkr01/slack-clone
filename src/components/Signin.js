import React, { Component } from 'react';

class Signin extends Component {
  render() {
    return (
      <div className="signin-form">
        <h2>Signup / Signin</h2>
        <button>Sign up / Sign in with Google</button>
      </div>
    );
  }
}

export default Signin;
